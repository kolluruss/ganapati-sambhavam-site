.DS_Store
__pycache__/
*.pyc

# One-off export for handing text to Vagdhenu — a point-in-time snapshot
# (depends on which verses already have audio when it's generated), not
# part of the deployed site. Regenerate with tools/export_vagdhenu_shards.py.
vagdhenu_shards/
